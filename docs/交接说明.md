# 交接说明 · WorkBuddy 积分工具

> 给下一位接手者(AI 或人)的**快速上手与状态快照**。先读这份,再看 `AGENTS.md` 与 `DEVELOPMENT.md`。

---

## 1. 项目是什么

多账号采集 WorkBuddy 积分并可视化。核心洞察:**积分包本身静态(总量/到期不变),变化的是"消耗"**——系统围绕"剩余积分变化"建模(历史快照 + 折线趋势 + 今日已用/今日会过期)。

- 仓库:`Simiely/workbuddy-credits-tool`(public)
- 当前版本:**v1.0.12**(CHANGELOG 有完整记录)
- 技术:Node ≥18 原生 ESM,**零第三方依赖**;前端原生 HTML/CSS/JS,无框架
- 双入口:CLI(`wb-credits.mjs`)+ GUI(`wb-gui.mjs`),共享 `lib/` 11 个模块

## 2. ⚠️ 三处部署位置(改代码必须三处同步)

| 位置 | 路径 | 用途 |
|---|---|---|
| **正式代码(源)** | `D:\workbuddy\2026-08-03-09-29-43\tools\` | **git 仓库根**,CLI/GUI 的源,改这里并 push |
| 工具中心副本 | `D:\workbuddy\2026-08-03-09-29-43\tools-center\tools\wb-credits-tool\` | 9090 平台托管实例(端口 8123) |
| 桌面规范包 | `E:\desktop\wb-credits-tool\` | 分发/自测接入用(含 tool.json) |

**同步规则**:改 `wb-gui.html/js` 后 `cp` 到另外两处(前端实时读文件,刷新即生效);改 `wb-gui.mjs`/`lib/*` 后同样同步**并重启对应服务进程**。

## 3. 运行实例与端口(2026-08-03 快照)

| 端口 | 进程 | 说明 |
|---|---|---|
| 8080 | `wb-gui.mjs`(正式代码) | 独立 GUI 实例(后台运行中) |
| 9090 | tools-center `server.mjs` | 工具中心平台(首页 + /tool/* 反代) |
| 8123 | `wb-gui.mjs`(副本) | 工具中心内的积分仪表盘 |
| 8129 | edge-daemon(工具中心托管) | 「添加当前账号」依赖;CDP 标准发现 |
| 9222 | Edge 调试端口 | **Edge 必须以 `--remote-debugging-port=9222` 启动** daemon 才能连上 |
| 9333 | — | 已废弃(旧 daemon 端口,勿再用) |

## 4. 关键数据文件(`tools/` 下,全部 gitignore,⚠️含凭证)

| 文件 | 内容 | 敏感性 |
|---|---|---|
| `wb-accounts.json` | 账号池 + 每账号 cookie 凭证(当前 **6 个账号**:鲁妈妈/坤坤/小陈/小黄/张妈妈/爸爸) | 🔴 严禁外传/上传 |
| `wb-history.json` | 消耗历史快照(500 条上限,同分钟去重) | 一般 |
| `wb-last-data.json` | 最近一次完整查询缓存(离线可看) | 一般 |
| `wb-sync.json` | WebDAV 云同步配置(含密码,仅本机) | 🔴 仅本机 |

## 5. 常用命令

```bash
# GUI(独立实例)
node wb-gui.mjs                    # 8080;工具中心内由平台托管(8123)

# CLI
node wb-credits.mjs                # 单账号查询(markdown)
node wb-credits.mjs all            # 批量查询全部账号
node wb-credits.mjs accounts       # 账号池列表
node wb-credits.mjs save-current   # 把当前 Edge 登录账号存入账号池
node wb-credits.mjs rename <key> <名> / del <key>

# edge-daemon(仅添加账号需要;工具中心已托管 8129)
node edge-daemon.mjs 8129

# Edge 调试模式启动(daemon 连接前提)
msedge.exe --remote-debugging-port=9222 https://www.workbuddy.cn/profile/plans-usage
```

## 6. 核心约定(动了会坏,务必遵守)

1. **总剩余口径 = 体验版基础用量 + 赠送积分**,前端/后端/历史快照/仪表盘必须一致
2. **子路径挂载**:`__BASE__`(工具中心注入或自行检测),所有 API 用 `__BASE__ + "/api/.."`,资源用相对路径;独立运行 `__BASE__=""` 行为不变
3. **daemon 发现**:轮询 `GET :9222/json/version` 取 `webSocketDebuggerUrl`(勿改回读 DevToolsActivePort 文件,那是旧坑)
4. 前端 `wb-gui.html/js` **每次请求实时读文件** → 改前端刷新页面即可,无需重启;改 `wb-gui.mjs`/`lib/*` 才需重启
5. 刷新超时:服务端单账号 8s、前端 12s;按钮状态单点控制(`setBusy`),`finally` 必恢复
6. **不要补丁套补丁**:布局/逻辑优先用标准机制(如 flex 分组 + `margin-right:auto` 两端对齐),不要中间占位元素硬凑

## 7. 待办与遗留

- **tools-center 路线图**(另一仓库 `Simiely/tools-center`):M3 微信读书工作台服务化(可选)、M4 NAS 部署(用户有群晖,部署指南已写)
- Edge 添加账号:需调试模式启动 Edge(本机已配好,见上)
- `.sep` CSS 样式残留(无引用,可顺手清理)
- `legacy/` 目录留档(勿删);`README-架构.md`/`README-wb-credits.md` 是旧文档,已被新规范文档覆盖(gitignore,本地保留)

## 8. 文档地图

| 文档 | 给谁 | 内容 |
|---|---|---|
| `README.md` | 用户 | 是什么、快速开始、CLI 速查、数据隐私 |
| `AGENTS.md` | **AI/未来的你** | 技术栈、关键坑摘要、约定、命令 |
| `DEVELOPMENT.md` | 开发者 | 概览、架构摘要、**问题索引** |
| `CHANGELOG.md` | 所有人 | v1.0.0~v1.0.12 完整版本记录 |
| `rules/技术栈.md` | AI | 模块清单、数据文件、精确版本 |
| `rules/常见坑.md` | AI | 10 条坑(TL;DR 格式) |
| `docs/架构.md` | 开发者 | 完整架构、模块职责、API 一览 |
| `docs/问题记录/` | 开发者 | 10 篇,一坑一篇(knowledge-base 同模板) |
| **`docs/交接说明.md`** | **接手者** | 本文件,先读它 |
